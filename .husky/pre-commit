# Format only staged markdown files before commit.
files=$(git diff --cached --name-only --diff-filter=ACMR -- '*.md')

if [ -z "$files" ]; then
	exit 0
fi

echo "$files" | xargs npx prettier --write
echo "$files" | xargs git add
